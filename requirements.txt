# ── Production dependencies ───────────────────────────────────────────────────
# Pinned to avoid "works on my machine" issues across CI and production.
# These go into the Docker image (Stage 2).

fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==1.10.17         # v1 for FastAPI 0.111 compatibility

xgboost==2.0.3
scikit-learn==1.4.2
pandas==2.2.2
numpy==1.26.4

mlflow==2.13.0            # model loading via MODEL_URI
httpx==0.27.0             # async HTTP client used by FastAPI TestClient
joblib==1.4.2
